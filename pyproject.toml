[project]
name = "bambara-asr-leaderboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.13.0",
    "python-dotenv>=1.2.1",
    "ruff>=0.15.1",
    "torch>=2.10.0",
    "torchcodec>=0.10.0",
]
